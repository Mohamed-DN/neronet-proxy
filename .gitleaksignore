# Baseline of findings in history that are real but are not fixtures. One fingerprint
# per finding (commit:file:rule:line); no values. Remove a line when the code it
# points at is gone and the credential no longer works.
#
# console/backend/services/NukeEngine.js compares the credential supplied to the
# nuke flow with string literals, so a caller that knows a literal passes the check.
# The literals are still in the file on main. This baseline records that they are
# known; it does not neutralise them. Removing them is a code change, not a scanner
# setting.
706265ce0dd79597c9d88a8f1ec788a1c967ea0b:console/backend/services/NukeEngine.js:generic-api-key:556
d60b90aab857378c1643e2a2d70d05362b830efb:console/backend/services/NukeEngine.js:generic-api-key:489
d60b90aab857378c1643e2a2d70d05362b830efb:console/backend/services/NukeEngine.js:generic-api-key:499

# console/frontend/src/services/api.js: the offline demo login accepts a fixed
# credential. It exists only in the frontend's mock mode and is not checked by the
# backend.
d60b90aab857378c1643e2a2d70d05362b830efb:console/frontend/src/services/api.js:generic-api-key:1161
